training:
  batch_size: 2048
  lr: 0.001
  n_iter: 5000
  sample_every: 1000
  n_samples: 4096

model:
  noise_dim: 32
  hidden_dim: 256
  out_dim: 2
  temp: 0.05

data:
  dataset: "8gaussians" # or "checkerboard"
